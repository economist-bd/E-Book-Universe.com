// Example client-side config file (safe to commit).
// Copy this file to config.js locally and replace the placeholder with your real API key.
// NOTE: config.js is listed in .gitignore so it will not be committed.

window.APP_CONFIG = {
  // Replace this with your real API key in your local config.js (do NOT commit it)
  GOOGLE_API_KEY: "YOUR_API_KEY_HERE"
};
